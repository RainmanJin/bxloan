package com.coamctech.bxloan.commons.utils.excel;

import org.apache.poi.ss.usermodel.Row;

public interface FunctionRowCell<T> {
	/**
	 * @param rownum 行号
	 * @param row Excel每一行
	 * @param t 数据
	 */
	void apply(int rownum,Row row,T t);
}
